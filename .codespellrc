[codespell]
skip = package-lock.json,*.svg,*.png,*.jpg,*.jpeg,*.gif,*.ico,dist,node_modules,storybook-static,coverage
# vitest's afterAll hook reads as "after all" to codespell.
ignore-words-list = afterall
