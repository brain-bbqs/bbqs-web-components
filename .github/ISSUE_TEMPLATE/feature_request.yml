name: Feature request
description: Suggest something to add to or generalize into a @brain-bbqs package
body:
  - type: textarea
    id: description
    attributes:
      label: Describe the feature
      description: What should be shared, which apps already carry a copy of it, and where they differ.
    validations:
      required: true
